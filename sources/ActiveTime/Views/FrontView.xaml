<UserControl x:Class="DustInTheWind.ActiveTime.Views.FrontView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:recording="clr-namespace:DustInTheWind.ActiveTime.Common.Recording;assembly=ActiveTime.Common"
             xmlns:cmd="clr-namespace:Microsoft.Practices.Prism.Commands;assembly=Microsoft.Practices.Prism"
             xmlns:converters="clr-namespace:DustInTheWind.ActiveTime.Converters"
             xmlns:commands="clr-namespace:DustInTheWind.ActiveTime.Commands"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Loaded="MainView_OnLoaded">

    <Grid Margin="5">
        <Grid.Resources>

            <!--<ui:RecordListConverter x:Key="RecordListConverter" />-->
            <converters:RecordConverter x:Key="RecordConverter" />
            <!--<DataTemplate DataType="{x:Type recording:DayTimeInterval}">
                <Label Padding="10,0,0,0"
                                Content="{Binding Converter={StaticResource ResourceKey=RecordConverter}}" />
            </DataTemplate>
            <DataTemplate DataType="{x:Type recording:Break}">
                <TextBlock Padding="0,0,10,0" HorizontalAlignment="Right"
                           Background="Azure"
                                Text="{Binding Converter={StaticResource ResourceKey=RecordConverter}}" />
            </DataTemplate>-->

        </Grid.Resources>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="90" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0"
               Content="Date:"
               Margin="3" />

        <DatePicker Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="3"
                    Margin="3" HorizontalAlignment="Left" MinWidth="150"
                    SelectedDate="{Binding Path=Date, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                    SelectedDateFormat="Long"/>

        <Label Grid.Row="1" Grid.Column="0"
               Content="Active Time:"
               Margin="3" />

        <TextBox Grid.Row="1" Grid.Column="1"
                 Margin="3"
                 VerticalAlignment="Center"
                 Text="{Binding Path=ActiveTime, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}"
                 IsReadOnly="True" />

        <Label Grid.Row="2" Grid.Column="0"
               Content="Total Time:"
               Margin="3" />

        <TextBox Grid.Row="2" Grid.Column="1"
                 Margin="3"
                 VerticalAlignment="Center"
                 Text="{Binding Path=TotalTime, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}"
                 IsReadOnly="True" />

        <Label Grid.Row="1" Grid.Column="2"
               Content="Begin:"
               Margin="3" />

        <TextBox Grid.Row="1" Grid.Column="3"
                 Margin="3"
                 VerticalAlignment="Center"
                 Text="{Binding Path=BeginTime, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}"
                 IsReadOnly="True" />

        <Label Grid.Row="2" Grid.Column="2"
               Content="Estimated End:"
               Name="label6"
               Margin="3" />

        <TextBox Grid.Row="2" Grid.Column="3"
                 Margin="3"
                 Name="textBoxEstimatedEndTime"
                 VerticalAlignment="Center"
                 Text="{Binding Path=EstimatedEndTime, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}"
                 IsReadOnly="True" />

        <Label Grid.Row="3" Grid.Column="0"
               Content="Records:"
               Name="label3"
               Margin="3" />

        <ListBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="3"
                 Margin="3"
                 x:Name="listBox1"
                 ItemsSource="{Binding Path=Records}"
                 SelectionMode="Extended">

            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type recording:DayTimeInterval}">
                    <TextBlock Padding="10,1,0,1" Text="{Binding Converter={StaticResource ResourceKey=RecordConverter}}" />
                </DataTemplate>
            </ListBox.ItemTemplate>

            <ListBox.ContextMenu>
                <ContextMenu Name="contextMenuRecords" Opened="contextMenuRecords_Opened">
                    <MenuItem Name="menuItemNew" Header="New" Click="menuItemNew_Click" IsEnabled="False"/>
                    <Separator Margin="0"/>
                    <MenuItem Name="menuItemMerge" Header="Merge" Click="menuItemMerge_Click" IsEnabled="False"/>
                    <MenuItem Name="menuItemSplit" Header="Split" Click="menuItemSplit_Click" IsEnabled="False"/>
                    <Separator Margin="0"/>
                    <MenuItem Name="menuItemDelete" Header="Delete"
                                commands:MenuItemClick.Command="{Binding DeleteCommand}"
                                commands:MenuItemClick.CommandParameter="{Binding ElementName=listBox1, Path=SelectedItem, UpdateSourceTrigger=Explicit}"/>
                </ContextMenu>
            </ListBox.ContextMenu>

        </ListBox>

        <StackPanel Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="3" Margin="5" Name="stackPanel2" Orientation="Horizontal">

            <Button Content="Comments" MinWidth="75" Name="buttonComments" Margin="0,0,5,0" cmd:Click.Command="{Binding CommentsCommand}"/>
            <Button Content="Refresh" Margin="0,0,5,0" Name="buttonRefresh" cmd:Click.Command="{Binding RefreshCommand}" MinWidth="75" />

            <!--<MenuItem Header="Delete2"
                              customcmd:MenuItemClick.Command="{Binding DeleteCommand}"
                              customcmd:MenuItemClick.CommandParameter="{Binding ElementName=listBox1, Path=SelectedItem, Mode=TwoWay}"/>-->

            <!--<Button Content="click"
                    cmd:Click.Command="{Binding DeleteCommand}"
                    cmd:Click.CommandParameter="{Binding ElementName=listBox1, Path=SelectedItem}"/>-->

        </StackPanel>
    </Grid>

</UserControl>
